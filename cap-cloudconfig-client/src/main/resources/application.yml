server:
  port: 8002

spring:
  application:
    name: spring-cloud-config-client

# spring boot actuator
info:
  app:
    name: com.iisigroup.cap.cap-cloudconfig-client
    description: Cap4 spring-cloud-config-client
    version: 4.2.0
management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info
